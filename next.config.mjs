/** @type {import('next').NextConfig} */
const nextConfig = {
  // remplace l’ancienne experimental.serverComponentsExternalPackages par:
  serverExternalPackages: [], // ou retire si inutile
}
export default nextConfig
